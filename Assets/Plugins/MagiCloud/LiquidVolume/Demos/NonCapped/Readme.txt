How to implement a non-capped container:

Liquid Volume is designed to work on closed containers. But you can use an open flask for instance and put a liquid volume inside with detail set to "Default No Flask" as shown in this demo scene.